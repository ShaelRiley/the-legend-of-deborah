LOD = LOD or {}

-- RETIRED: the former level-banded JIP kit (SMG + Shotgun, then Magnum/AR2)
-- conflicted with the now-authoritative first-deployment staging system. Every
-- newly admitted cooperative identity receives the same baseline Pistol/Crowbar
-- loadout and one persistent individualized peer firearm in the Flatgrass hut.
-- Keep this file as a harmless compatibility marker until the broader audit
-- removes retired include names from old documentation and external test scripts.
LOD.LootCatchupRetired = true
