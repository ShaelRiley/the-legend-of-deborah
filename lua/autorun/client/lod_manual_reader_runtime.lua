-- Retired: the canonical reader now lives in lod/cl_instruction_manual.lua.
-- Leave this inert shim so existing installations overwrite the old autorun.
hook.Remove("Think", "LOD_FieldManualDocumentScroll")
