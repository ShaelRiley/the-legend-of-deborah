# Bribe removed from active gameplay

Baseline: verified published P1 `08d887ba5ffaf07c25bea2d400fb7cacc536ebd0`.
The author calls this “Brive”; the unique corresponding event is Bribe,
`bribe_blockade`. This is an explicit removal for now, not an assertion that
a particular payment defect was reproduced or repaired.

Finite gate: no active registration/production selection/developer preview, no
loaded payment network receiver or client review dialog; preserve other events,
exact non-exploding1d4 count, distinct types, rare fourth slot, deterministic
streams, route safety, reset/rejoin ownership and resource limits.

Removed the server blockade/payment includes, client dialog include and its
AddCSLuaFile distribution. Normal cold startup cannot register or select the
event, create its terminal/cache/barrier, or accept payment packets. Unloaded
implementation and historical tests remain as dormant rollback material, not
a hidden setting or active preview. Existing shared entity/HUD fallback branches
are harmless without an event instance. Apply by fully restarting GMod/server
when later authorized; this does not migrate an already running old dungeon.
No deployment, cash refunds or persistent-data modifications were performed.

Live GDD00→01→05 LOD-EVENT-BRIBE-001 now explicitly removes the event and marks
its former collateral/payment design historical. Verified readback; document
revision ANLCKQmg2sNmtHp5e_z-25s8fbeyF1h9eaXLiqGeI4-plgc-IB2WBvfCJZ5LLh6J6h-cJN1SN2JarJRL8cf2MycVa-ecOt9zmMNrDjRnMg.
Re-enabling requires a new explicit author decision, including during next week's
Event System work. Other normalized rules and deferred scope remain unchanged.

The active catalog now has7 archetypes before Dungeon Level5,8 thereafter.
Seeded event combinations intentionally change when the catalog loses an entry;
the count law and isolated RNG domains do not. The manual's event list and Bribe
chapter were reconciled and generated offline/client payloads rebuilt together.
The manual now has163 chapters;150 feats and60 properties remain unchanged.

Fresh focused evidence: current full-catalog generation gate tied to actual boot
includes, missing Bribe registry/payment receiver/client startup, explicit
unknown-preview rejection,10 accepted quiz builds,0 bounded rejects, all8 remaining
archetypes, all4 d4 counts, real encounter reservations, ordered route proofs,
deterministic retry, failed native creation, late join and exact teardown.
Manual parity/search/bookmark/zoom/keyboard checks pass. Logs: BRIBE_REMOVAL_QUIZ.log
and BRIBE_REMOVAL_MANUAL.log. The two Bribe-only suites are retained on disk but
retired from the active release matrix; the older Skeleton mixed-catalog suite
is explicitly labeled historical. No other gameplay gate was removed.

Fresh canonical integration: **219 suites passed, zero failures**. Log:
BRIBE_REMOVAL_INTEGRATION.log, SHA256 746d36874b9b4ca78d007dc9545ae354599dbf5d1a4045de7501d8356cba073f.
The count is221 minus the two explicitly retired Bribe-only suites. Headless checks do
not certify native play. Native post-restart event sampling remains in the
planned human playtest. P1 branding equivalence/timings are inherited from its
verified commit; no branding, hull, concrete, gate or resource code changed here.

Resume: low-end profiling of repeated deterministic-noise hashing inside the
existing placement scan, using P1's fixed manifests and exact receipts. Measure
first; optimize only if justified, one bottleneck. Then focused fatal/game-breaking
audit→native playtest. Keep every unfinished Crate exit in GREAT_CRATE_C3.md.
Big Loot→Event System→comprehensive audit stay deferred September28–October4.
